will go with apis
	-> Docker, Github
	-> create apis list
	-> create yaml resources (namespace, configmap, secret, deployment, service)
	-> Kubernetes
	-> groovy jinja templating


// data.toLowerCase().replaceAll("[.|,|_| ]", '-')
-------------------
will check environment or global variable name: drivedFromTemplate: true/false

so either drive name from globalvariable file
--------------------
maven-> install, test